import React from 'react';
import { MediaSection as MediaSectionMain } from '../../../../pages/admin/addProduct/MediaSection';

export const MediaSection = (props) => {
  return <MediaSectionMain {...props} />;
};

export default MediaSection;